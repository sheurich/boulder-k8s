# Nonce Service A (taro)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: boulder-nonce-a
  labels:
    app.kubernetes.io/name: boulder-nonce-a
    app.kubernetes.io/component: nonce-service
    app.kubernetes.io/part-of: boulder
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: boulder-nonce-a
  template:
    metadata:
      labels:
        app.kubernetes.io/name: boulder-nonce-a
        app.kubernetes.io/component: nonce-service
        app.kubernetes.io/part-of: boulder
    spec:
      containers:
        - name: boulder-nonce
          image: letsencrypt/boulder:latest
          args:
            - boulder
            - nonce-service
            - --config
            - /config/nonce-a.json
            - --addr
            - $(POD_IP):9101
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          ports:
            - name: grpc
              containerPort: 9101
              protocol: TCP
            - name: metrics
              containerPort: 8021
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /debug/vars
              port: 8111
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /debug/vars
              port: 8111
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            - name: ipki-ca
              mountPath: /certs/ipki/ca.crt
              subPath: ca.crt
              readOnly: true
            - name: ipki-service
              mountPath: /certs/ipki/nonce-a
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: boulder-nonce-a-config
        - name: ipki-ca
          secret:
            secretName: boulder-internal-ca
        - name: ipki-service
          secret:
            secretName: boulder-nonce-a-grpc-tls
---
apiVersion: v1
kind: Service
metadata:
  name: boulder-nonce-a
  labels:
    app.kubernetes.io/name: boulder-nonce-a
    app.kubernetes.io/component: nonce-service
spec:
  clusterIP: None
  ports:
    - name: grpc
      port: 9101
      targetPort: grpc
      protocol: TCP
    - name: metrics
      port: 8021
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: boulder-nonce-a
---
# Nonce Service B (zinc)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: boulder-nonce-b
  labels:
    app.kubernetes.io/name: boulder-nonce-b
    app.kubernetes.io/component: nonce-service
    app.kubernetes.io/part-of: boulder
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: boulder-nonce-b
  template:
    metadata:
      labels:
        app.kubernetes.io/name: boulder-nonce-b
        app.kubernetes.io/component: nonce-service
        app.kubernetes.io/part-of: boulder
    spec:
      containers:
        - name: boulder-nonce
          image: letsencrypt/boulder:latest
          args:
            - boulder
            - nonce-service
            - --config
            - /config/nonce-b.json
            - --addr
            - $(POD_IP):9102
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          ports:
            - name: grpc
              containerPort: 9102
              protocol: TCP
            - name: metrics
              containerPort: 8022
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /debug/vars
              port: 8112
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /debug/vars
              port: 8112
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            - name: ipki-ca
              mountPath: /certs/ipki/ca.crt
              subPath: ca.crt
              readOnly: true
            - name: ipki-service
              mountPath: /certs/ipki/nonce-b
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: boulder-nonce-b-config
        - name: ipki-ca
          secret:
            secretName: boulder-internal-ca
        - name: ipki-service
          secret:
            secretName: boulder-nonce-b-grpc-tls
---
apiVersion: v1
kind: Service
metadata:
  name: boulder-nonce-b
  labels:
    app.kubernetes.io/name: boulder-nonce-b
    app.kubernetes.io/component: nonce-service
spec:
  clusterIP: None
  ports:
    - name: grpc
      port: 9102
      targetPort: grpc
      protocol: TCP
    - name: metrics
      port: 8022
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: boulder-nonce-b
