apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Observability component - adds Prometheus ServiceMonitors and PodMonitors
# Requires Prometheus Operator CRDs to be installed
#
# Usage: Add to overlay's components list:
#   components:
#     - ../../components/observability

resources:
  - servicemonitors.yaml
