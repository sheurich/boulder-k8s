# Redis password secret for Boulder services
apiVersion: v1
kind: Secret
metadata:
  name: boulder-redis-password
  labels:
    app.kubernetes.io/name: boulder
    app.kubernetes.io/component: redis
type: Opaque
stringData:
  # Must match Redis helm values password
  redis_password: boulder-redis-dev
